node_modules/
dist/
loader/
hydrate/
www/
src/components.d.ts

# Framework adapters — generated by the output targets in stencil.config.ts.
# Everything else under adapters/ (manifests, tsconfigs, READMEs) is committed.
/adapters/react/src/
/adapters/vue/src/
/adapters/svelte/src/
/adapters/angular/src/lib/
.stencil/
*.log
.DS_Store
.playwright-cli/
css/
cdn/

# Vendored agent skills, fetched from third-party repos (see skills-lock.json,
# which pins each one by source + hash). Not ours to republish under this
# repo's licence, and reproducible from the lock file — so the lock file is
# tracked and the payload is not.
/.agents/

# Symlinks into /.agents/ — broken in any fresh clone, since the payload above
# is not tracked. Re-created by the skills installer alongside it.
/.claude/skills/
